# Merge PDF 工具页技术文档 v3.2

> 本文档为 Merge PDF 插件化工具站的技术标准。
> 前端、后端、插件系统、SEO 完整覆盖。

---

# 0. 工具页核心概念

**工具页不是硬编码页面**，所有内容来自插件系统的 3 个 JSON：

| 文件          | 作用                                | 工具页是否依赖？ |
| ------------- | ----------------------------------- | ---------------- |
| plugin.json   | 工具基础信息（slug/category/apiPath） | ✔ 必须           |
| ui.json       | 标题、描述、FAQ、SEO、多语言文案      | ✔ 必须           |
| schema.json   | 工具 UI 结构（上传、选项、按钮）      | ✔ 必须           |

工具页的任务：

✔ 根据 URL 解析 slug / category / locale
✔ 调用插件系统 API 获取插件数据
✔ 根据 schema.json 动态渲染选项
✔ 根据 ui.json 渲染标题、描述、FAQ
✔ 根据 plugin.json 触发 API 调用
✔ SEO 自动生成

---

# 1. 路由结构

```
/{locale}/{categoryId}/{slug}

示例：
/zh/pdf-tools/merge-pdf
/en/pdf-tools/merge-pdf
```

| 参数       | 说明              | 示例                      |
| ---------- | ----------------- | ------------------------- |
| locale     | 语言代号          | en / zh                   |
| categoryId | 工具分类          | pdf-tools                 |
| slug       | 工具唯一标识      | merge-pdf                 |

---

# 2. 目录结构

```
Merge-PDF/
├── backend/                              # Express.js 后端服务
│   ├── src/
│   │   ├── app.ts                       # 应用入口
│   │   ├── plugin-loader.ts             # 插件加载器
│   │   └── routes/
│   │       ├── plugins.ts               # 插件管理 API
│   │       ├── dynamic.ts               # 动态路由生成
│   │       └── pdf.ts                   # PDF 处理
│   ├── plugins/
│   │   └── pdf-merge/                   # PDF 合并插件
│   │       ├── plugin.json              # 插件配置
│   │       ├── schema.json              # 数据模式定义
│   │       ├── ui.json                  # UI 文本和配置
│   │       └── handler/                 # 处理函数
│   └── package.json
│
├── frontend/pdf-tools/                  # Next.js 14 前端应用
│   ├── src/
│   │   ├── app/[locale]/                # 国际化路由
│   │   │   ├── layout.tsx               # 根布局
│   │   │   └── [category]/[slug]/       # 动态工具页面
│   │   │       └── page.tsx             # 页面组件（SSR）
│   │   ├── components/                  # React 组件库
│   │   │   ├── ToolClient.tsx           # 主工具客户端（核心）
│   │   │   ├── CoreToolArea.tsx         # 文件上传和处理区域
│   │   │   ├── UseCaseCards.tsx         # 用例选项卡片（动态渲染）
│   │   │   ├── ResultPage.tsx           # 结果展示页面
│   │   │   ├── HowToSection.tsx         # 操作指南
│   │   │   ├── FAQSection.tsx           # 常见问题
│   │   │   ├── InlineFeedback.tsx       # 反馈提示
│   │   │   ├── Breadcrumb.tsx           # 面包屑导航
│   │   │   ├── SchemaRenderer.tsx       # 动态表单渲染器
│   │   │   └── layout/
│   │   │       ├── Header.tsx           # 页头（sticky）
│   │   │       └── Footer.tsx           # 页脚
│   │   ├── lib/
│   │   │   ├── api.ts                   # 后端 API 调用
│   │   │   └── pdfMerger.ts             # 工具函数
│   │   └── types/index.ts               # TypeScript 类型定义
│   └── package.json
│
├── docs/                                # 文档
└── docker-compose.yml                   # Docker 编排
```

---

# 3. 工具页页面结构（UI Blueprint）

```
┌─────────────────────────────┐
│     Header (sticky)         │  Merge PDF 品牌 + 语言切换
├─────────────────────────────┤
│  Home > Merge PDF > 当前工具 │  Breadcrumb
├─────────────────────────────┤
│   Icon（emoji）             │
│   H1 标题（渐变文字）        │
│   描述（subtitle）          │
│   特性（features）          │
├─────────────────────────────┤
│  CoreToolArea               │
│   ├ 上传区域（拖拽/点击）   │
│   ├ 文件列表（可排序）      │
│   ├ 页面范围输入            │
│   └ 提交按钮                │
├─────────────────────────────┤
│  ResultPage（合并成功后）   │
│   ├ 下载按钮                │
│   └ 重新开始                │
├─────────────────────────────┤
│  UseCaseCards（3列横排）    │
│   └ 从 schema.options 生成  │
├─────────────────────────────┤
│  HowToSection               │
├─────────────────────────────┤
│  FAQSection                 │
├─────────────────────────────┤
│           Footer            │
└─────────────────────────────┘
```

---

# 4. 插件配置文件

## 4.1 plugin.json

```json
{
  "name": "Merge PDF",
  "slug": "merge-pdf",
  "category": "pdf",
  "apiPath": "/api/pdf/merge-pdf",
  "order": 1
}
```

## 4.2 schema.json

```json
{
  "upload": {
    "multiple": true,
    "types": ["application/pdf"],
    "maxSize": 104857600,
    "maxFiles": 20
  },
  "options": [
    {
      "name": "optimizeForPrint",
      "type": "checkbox",
      "default": false,
      "useCaseKey": "printing",
      "label": { "en": "Optimize for Print", "zh": "优化打印" }
    },
    {
      "name": "keepBookmarks",
      "type": "checkbox",
      "default": false,
      "useCaseKey": "bookmarks",
      "label": { "en": "Keep Bookmarks", "zh": "保留书签" }
    },
    {
      "name": "usePageRange",
      "type": "checkbox",
      "default": false,
      "useCaseKey": "pageRange",
      "label": { "en": "Use Page Range", "zh": "使用页面范围" }
    }
  ],
  "submitText": {
    "en": "Merge PDF Files",
    "zh": "合并 PDF 文件"
  },
  "outputType": "file"
}
```

**关键字段说明：**
- `useCaseKey`: 关联 ui.json 中的 useCases 展示内容，用于动态渲染 UseCaseCards

## 4.3 ui.json 结构

```json
{
  "categoryName": { "en": "Merge PDF", "zh": "合并 PDF" },
  "title": { "en": "...", "zh": "..." },
  "description": { "en": "...", "zh": "..." },
  "h1": { "en": "...", "zh": "..." },
  "icon": "📄",
  "subtitle": { "en": "...", "zh": "..." },
  "features": { "en": "...", "zh": "..." },
  "breadcrumb": {
    "home": { "en": "Home", "zh": "首页" },
    "current": { "en": "Merge PDF", "zh": "合并 PDF" }
  },
  "upload": { ... },
  "fileList": { ... },
  "actions": { ... },
  "feedback": { ... },
  "useCases": {
    "sectionTitle": { "en": "...", "zh": "..." },
    "printing": {
      "title": { "en": "...", "zh": "..." },
      "description": { "en": "...", "zh": "..." },
      "features": { "1": {...}, "2": {...}, "3": {...} },
      "action": { "en": "...", "zh": "..." }
    },
    "bookmarks": { ... },
    "pageRange": { ... }
  },
  "howTo": { ... },
  "faq": { ... },
  "result": { ... },
  "footer": { ... }
}
```

---

# 5. 核心组件

## 5.1 Header.tsx（页头）

路径：`frontend/pdf-tools/src/components/layout/Header.tsx`

**关键样式：**
```tsx
<header className="bg-white border-b border-gray-200 sticky top-0 z-50">
```

- `sticky top-0`: 滚动时固定在顶部
- `z-50`: 确保在其他元素之上

## 5.2 page.tsx（SSR 页面）

路径：`frontend/pdf-tools/src/app/[locale]/[category]/[slug]/page.tsx`

职责：
1. 解析 URL 参数（locale, category, slug）
2. 调用插件 API 获取数据
3. 生成 SEO 元信息
4. 渲染 ToolClient 组件

```typescript
export async function generateMetadata({ params }: PageProps): Promise<Metadata> {
  const { locale, category, slug } = await params;
  const plugin = await fetchPluginData(slug, locale);
  // 返回 SEO 元信息
}

export default async function ToolPage({ params }: PageProps) {
  const { locale, category, slug } = await params;
  const plugin = await fetchPluginData(slug, locale);
  if (!plugin) notFound();

  return <ToolClient pluginData={plugin} locale={locale} categoryId={category} slug={slug} />;
}
```

## 5.3 ToolClient.tsx（核心客户端组件）

路径：`frontend/pdf-tools/src/components/ToolClient.tsx`

职责：
1. 从 schema.options 动态初始化选项状态
2. 从 ui.categoryName 动态获取分类名称
3. 管理文件列表、上传、分析、排序
4. 调用后端 API 进行合并
5. 显示结果和反馈

**关键代码：**
```typescript
// 动态初始化选项状态
const initialOptions = useMemo(() => {
  const opts: Record<string, any> = {};
  pluginData.schema?.options?.forEach(opt => {
    opts[opt.name] = opt.default ?? false;
  });
  return opts;
}, [pluginData.schema]);

// 动态获取分类名称（后端已本地化，直接使用字符串）
const categoryName = pluginData.ui?.categoryName || categoryId;
```

**H1 标题样式（渐变文字）：**
```tsx
<h1 className="text-5xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent mb-4 pb-2">
  {t('h1')}
</h1>
```

- `bg-clip-text text-transparent`: 渐变文字效果
- `pb-2`: 防止字母下降部分（如 g, y）被裁剪

## 5.4 UseCaseCards.tsx（动态选项卡片）

路径：`frontend/pdf-tools/src/components/UseCaseCards.tsx`

职责：
1. 从 schema.options 读取选项列表
2. 过滤有 useCaseKey 的选项
3. 从 ui.useCases[useCaseKey] 读取展示内容
4. 动态渲染选项卡片（3列横排）

**关键代码：**
```typescript
interface UseCaseCardsProps {
  schemaOptions: PluginSchemaOption[];
  options: Record<string, any>;
  onOptionsChange: (options: Record<string, any>) => void;
  ui: PluginUI;
  locale: string;
}

// 本地化文本处理（兼容后端已本地化的数据）
const getLocalizedText = (obj: any) => {
  if (!obj) return '';
  if (typeof obj === 'string') return obj;  // 后端已本地化
  return obj[locale] || obj['en'] || '';
};

// 3列横排布局（必须使用静态类名，Tailwind 不支持动态类名）
<div className="grid grid-cols-1 md:grid-cols-3 gap-6">
  {useCaseOptions.map(opt => {
    const useCaseData = ui.useCases?.[opt.useCaseKey!];
    return <CardItem>...</CardItem>;
  })}
</div>
```

**注意事项：**
- Tailwind CSS 不支持动态类名如 `md:grid-cols-${n}`，必须使用静态类名
- 文字颜色使用 inline style 确保可见性

---

# 6. 后端本地化机制

## 6.1 getLocalizedUI 函数

路径：`backend/src/plugin-loader.ts`

后端 API 返回数据时，会自动将多语言对象转换为单一字符串：

```typescript
export function getLocalizedUI(ui: PluginUI, lang: string): any {
  const result: any = {};
  for (const key in ui) {
    const value = ui[key];
    if (typeof value === 'object' && value !== null) {
      if (value[lang] !== undefined) {
        result[key] = value[lang];  // 提取指定语言的字符串
      } else if (value['en'] !== undefined) {
        result[key] = value['en'];  // 回退到英文
      } else {
        result[key] = getLocalizedUI(value, lang);  // 递归处理嵌套对象
      }
    } else {
      result[key] = value;
    }
  }
  return result;
}
```

**转换示例：**
```json
// ui.json 原始数据
{ "title": { "en": "Merge PDF", "zh": "合并 PDF" } }

// API 返回数据（lang=en）
{ "title": "Merge PDF" }
```

**前端处理：**
由于后端已本地化，前端 `getLocalizedText` 需要同时处理字符串和对象：
```typescript
const getLocalizedText = (obj: any) => {
  if (!obj) return '';
  if (typeof obj === 'string') return obj;  // 后端已本地化
  return obj[locale] || obj['en'] || '';
};
```

---

# 7. API 端点

| 端点                           | 方法 | 说明               |
| ------------------------------ | ---- | ------------------ |
| `/api/health`                  | GET  | 健康检查           |
| `/api/plugins`                 | GET  | 获取所有插件列表   |
| `/api/plugins/:slug`           | GET  | 获取单个插件配置   |
| `/api/pdf/merge-pdf`           | POST | 合并 PDF 文件      |
| `/api/pdf/merge-pdf/analyze`   | POST | 分析 PDF 文件      |

## API 调用格式

```typescript
// 合并 PDF
POST /api/pdf/merge-pdf
Content-Type: multipart/form-data

FormData:
- files[]: PDF 文件（多个）
- options: JSON 字符串 { keepBookmarks, optimizeForPrint, usePageRange }
- pageRanges: JSON 字符串（可选）

Response: Blob (application/pdf)
```

---

# 8. 类型定义

路径：`frontend/pdf-tools/src/types/index.ts`

```typescript
export interface PluginSchemaOption {
  name: string;
  type: 'boolean' | 'number' | 'text' | 'select' | 'checkbox';
  default?: any;
  useCaseKey?: string;  // 关联 ui.useCases 的 key
  label: { [lang: string]: string };
  choices?: Array<{ value: string; label: { [lang: string]: string } }>;
}

export interface PluginSchema {
  upload: { multiple: boolean; types: string[]; maxSize: number; maxFiles: number };
  options: PluginSchemaOption[];
}

export interface PluginData {
  config: PluginConfig;
  ui: PluginUI;
  schema: PluginSchema;
}
```

---

# 9. 国际化（i18n）

工具页面所有内容来自插件 JSON：

- title / description / h1 / subtitle / features
- breadcrumb
- upload / fileList / actions / feedback
- useCases（动态选项卡片）
- howTo / faq / result / footer

**后端本地化：**
API 根据 `lang` 参数自动返回对应语言的字符串。

**前端兼容处理：**
```typescript
const getLocalizedText = (obj: any) => {
  if (!obj) return '';
  if (typeof obj === 'string') return obj;  // 后端已本地化
  return obj[locale] || obj['en'] || '';
};
```

---

# 10. 下载文件命名规则

```
{slug}-pdf-tools-{timestamp}.pdf

示例：merge-pdf-pdf-tools-1703520000000.pdf
```

---

# 11. 工具页面准则

✔ 工具页必须完全依赖插件系统
✔ 不允许硬编码任何工具名称
✔ 不允许硬编码分类名称（从 ui.categoryName 读取）
✔ slug 不存在 → 404
✔ ui.json 决定所有文案
✔ schema.json 决定选项结构
✔ schema.options.useCaseKey 关联 ui.useCases 展示内容
✔ plugin.json 决定 API path
✔ Tailwind 类名必须是静态的，不支持动态拼接

**工具页 = "读插件 → 动态渲染" 的纯展示层**

---

# 12. 添加新插件步骤

1. 在 `backend/plugins/` 创建新目录（如 `pdf-split`）
2. 添加 `plugin.json`：定义 name, slug, category, apiPath
3. 添加 `schema.json`：定义 upload, options（含 useCaseKey）, submitText
4. 添加 `ui.json`：定义所有多语言文案，包括 useCases
5. 添加 `handler/index.ts`：实现处理函数
6. 重启后端，系统自动加载和注册路由
7. 前端无需修改，自动支持新插件

---

# 13. 技术栈

| 层级       | 技术                | 版本     |
| ---------- | ------------------- | -------- |
| 前端框架   | Next.js             | 14.2.x   |
| 前端库     | React               | 18.3.x   |
| 样式       | Tailwind CSS        | 3.4.x    |
| 国际化     | next-intl           | 3.22.x   |
| 文件上传   | react-dropzone      | 14.2.x   |
| 后端框架   | Express.js          | 4.18.x   |
| PDF 处理   | pdf-lib             | 1.17.x   |
| 语言       | TypeScript          | 5.x      |

---

# 14. 已知问题与解决方案

## 14.1 渐变文字下降部分被裁剪

**问题：** 使用 `bg-clip-text text-transparent` 的渐变文字，字母下降部分（如 g, y, p）可能被裁剪。

**解决：** 添加 `pb-2` 内边距。

```tsx
<h1 className="... bg-clip-text text-transparent pb-2">
```

## 14.2 Tailwind 动态类名不生效

**问题：** `md:grid-cols-${n}` 等动态类名在生产环境不生效。

**原因：** Tailwind 在构建时静态分析类名，动态拼接的类名不会被包含。

**解决：** 使用静态类名。

```tsx
// ❌ 错误
<div className={`md:grid-cols-${count}`}>

// ✅ 正确
<div className="md:grid-cols-3">
```

## 14.3 UseCaseCards 文字不显示

**问题：** UseCaseCards 中的标题、描述、功能列表不显示。

**原因：** 后端 `getLocalizedUI` 已将多语言对象转换为字符串，前端 `getLocalizedText` 试图访问 `string['en']` 返回 undefined。

**解决：** `getLocalizedText` 需要同时处理字符串和对象。

```typescript
const getLocalizedText = (obj: any) => {
  if (!obj) return '';
  if (typeof obj === 'string') return obj;  // 后端已本地化
  return obj[locale] || obj['en'] || '';
};
```
