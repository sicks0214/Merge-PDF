# 前端环境变量

# 主站 URL（用于面包屑导航跳转）
# 生产环境留空（通过 Nginx 代理使用相对路径）
# 开发环境可设置为 http://localhost:3000
NEXT_PUBLIC_MAIN_APP_URL=

# API 基础路径
# 生产环境留空（默认 /api，通过 Nginx 代理）
# 开发环境可设置为 http://localhost:8000/api
NEXT_PUBLIC_API_URL=
